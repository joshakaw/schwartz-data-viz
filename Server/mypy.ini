[mypy]
# Point mypy to your source directories
# This tells mypy to analyze all files under the 'Server' directory
files = Server/

# You can also tell it to allow untyped imports for actual third-party libraries
# that don't have stubs, but for your own code, you want strictness.
# ignore_missing_imports = True # Only for *actual* missing third-party stubs

# For more verbose output to debug paths:
# show_column_numbers = True
# show_error_context = True
# verbose = True
